import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  // Start simple — you can paste your Figma tokens here later
  palette: {
    primary: { main: "#355C7D" },
    secondary: { main: "#6C5B7B" },
  },
});